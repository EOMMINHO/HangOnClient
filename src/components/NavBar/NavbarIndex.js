import React from 'react';
import { Nav } from './NavbarElements';

const Navbar = ({ toggle }) => {
  return (
    <>
      <Nav>
        Hang On
      </Nav>
    </>
  );
};

export default Navbar;
